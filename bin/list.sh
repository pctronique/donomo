#for entry in `ls *.json`; do
    #if [ ! -e ${0%/*}/imports.list ]
    #then
    #    echo '' > ${0%/*}/imports.list
    #fi

    #valid = false

    #while read line  
    #do
    #    if [ $entry == $line ]
    #    then
    #        valid = true
    #    fi
    #done < ${0%/*}/imports.list

    #if [ ! $valid ]
    #then
    #    echo '' > ${0%/*}/imports.list
    #fi
#done

